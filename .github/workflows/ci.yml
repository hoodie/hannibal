name: CI

on:
  push:
    branches:
      - main 
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build with tokio
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --all --features "runtime-tokio anyhow" --no-default-features --verbose

      - name: Build with eyre
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --all --features "runtime-tokio eyre" --no-default-features --verbose

      - name: Run tests with async-std
        uses: actions-rs/cargo@v1
        with:
          command: test 
          args: --all --verbose

      - name: Run tests with tokio
        uses: actions-rs/cargo@v1
        with:
          command: test 
          args: --all --verbose --features "runtime-tokio anyhow" --no-default-features --verbose

      - name: Run tests with eyre
        uses: actions-rs/cargo@v1
        with:
          command: test 
          args: --all --verbose --features "runtime-tokio eyre" --no-default-features --verbose
